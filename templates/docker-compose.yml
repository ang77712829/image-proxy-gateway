services:
  angemedia-gateway:
    build: .
    env_file:
      - .env
    ports:
      - "9890:9890"
    environment:
      - PUBLIC_BASE_URL=${PUBLIC_BASE_URL:-http://localhost:9890}
    volumes:
      - angemedia-data:/data
    restart: unless-stopped

volumes:
  angemedia-data:
